super + y
    alacritty

super + e
        nemo

super + r
        rofi -show drun -show-icons

super + shift + {q,r}
        bspc {quit,wm -r}

super + {t,v,f}
	bspc node -t {tiled,floating,fullscreen}

Print
    flameshot gui

super + c
        bspc node -c

super + {1-9}
	bspc desktop -f '^{1-9}'

super + k
	{xkill}
